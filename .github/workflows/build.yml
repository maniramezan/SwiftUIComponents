name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    name: Format, Build, and Test
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
        with:
          fetch-depth: 0

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1.7.0
        with:
          xcode-version: '26.3'

      - name: Restore SwiftPM cache
        id: restore-swiftpm-cache
        uses: actions/cache/restore@v5.0.5
        with:
          path: |
            .build
            ~/Library/Caches/org.swift.swiftpm
            ~/Library/org.swift.swiftpm
          key: swiftpm-${{ hashFiles('Package.swift', '.swift-format', 'Scripts/validate.sh') }}
          restore-keys: |
            swiftpm-

      - name: Show Swift version
        run: swift --version

      - name: Verify Swift toolchain
        run: |
          case "$(swift --version)" in
            *"Apple Swift version 6.4"*) ;;
            *)
              echo "Expected Apple Swift 6.4 from Xcode 26.3."
              exit 1
              ;;
          esac

      - name: Prepare validation script
        run: chmod +x Scripts/validate.sh Scripts/coverage.sh Scripts/check-coverage.py

      - name: Run validation script
        run: ./Scripts/validate.sh

      - name: Check changed-line coverage
        if: github.event_name == 'pull_request'
        run: ./Scripts/coverage.sh changed origin/${{ github.base_ref }}

      - name: Check full coverage baseline
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: ./Scripts/coverage.sh full

      - name: Validate DocC (DesignSystem)
        run: |
          swift package generate-documentation \
            --target DesignSystem \
            --warnings-as-errors \
            --exclude-extended-types

      - name: Validate DocC (Components)
        run: |
          swift package generate-documentation \
            --target Components \
            --warnings-as-errors \
            --exclude-extended-types

      - name: Save SwiftPM cache
        if: ${{ always() }}
        uses: actions/cache/save@v5.0.5
        with:
          path: |
            .build
            ~/Library/Caches/org.swift.swiftpm
            ~/Library/org.swift.swiftpm
          key: swiftpm-${{ hashFiles('Package.swift', '.swift-format', 'Scripts/validate.sh') }}
